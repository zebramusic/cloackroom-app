import HandoverClient from "@/app/private/handover/HandoverClient";

export const metadata = { title: "Cloackroom — Handover report" };

export default function Page() {
  return <HandoverClient />;
}
